import 'package:flutter/material.dart';

class CreateDiscussion extends StatelessWidget {
  const CreateDiscussion({super.key, required this.forumId});

  final int forumId;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}